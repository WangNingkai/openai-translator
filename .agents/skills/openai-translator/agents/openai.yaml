interface:
  display_name: "Openai Translator"
  short_description: "Repo-specific patterns and workflows for openai-translator"
  default_prompt: "Use the openai-translator repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true